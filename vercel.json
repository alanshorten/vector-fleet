{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/extract.js": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/share/:token", "destination": "/share.html?token=:token" }
  ]
}
